{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "tap test/**/*.test.js",
    "build": "./node_modules/.bin/tsc",
    "start": "fastify start -l info built/app.js",
    "dev": "fastify start -w -l info -p 4000 -P built/app.js",
    "build-run": "npm run build && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server-fastify": "^3.0.0-alpha.3",
    "aws-sdk": "^2.747.0",
    "fastify": "^3.0.0",
    "fastify-autoload": "^3.0.2",
    "fastify-cli": "^2.2.0",
    "fastify-plugin": "^2.0.0",
    "graphql": "^15.3.0"
  },
  "devDependencies": {
    "tap": "^14.0.0"
  }
}
