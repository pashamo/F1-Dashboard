import styles from '../../styles/Home.module.css'

const LoginForm = (props) => {
  
  return (
    <div className={styles.container}>
      <p>Login Form Component</p>
    </div>
  );
};

export default LoginForm;